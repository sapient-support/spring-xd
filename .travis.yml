language: java

jdk:
  - oraclejdk8

services:
  - rabbitmq
  - redis-server

sudo: false

cache:
  directories:
  - $HOME/.gradle

env:
  - TERM=dumb

script:
  - travis_wait ./gradlew --debug clean build distZip -PtestLoggingStarted=true -PwikiBranch=origin/master

#test
